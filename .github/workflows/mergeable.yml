name: mergeable
on:
  pull_request:
    types:
      - labeled
      - unlabeled
#      - synchronize
#      - opened
#      - edited
#      - ready_for_review
#      - reopened
#      - unlocked
#  pull_request_review:
#    types:
#      - submitted
#  check_suite:
#    types:
#      - completed
  
jobs:
  mergeable:
    runs-on: ubuntu-latest
    steps:
      - name: Mergeable
      - uses: ygnoh/mergeable@v0.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
